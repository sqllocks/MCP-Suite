{
  "name": "mcp-tokenization-secure",
  "version": "2.0.0",
  "description": "Secure tokenization server with 256-bit encryption for PHI masking",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["mcp", "tokenization", "phi", "hipaa", "security"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
